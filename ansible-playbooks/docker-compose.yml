version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    restart: always

  loki:
    image: grafana/loki:latest
    ports:
      - "3100:3100"
    restart: always

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    restart: always
